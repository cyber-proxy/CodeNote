package effictive.testcase;

import java.util.ArrayList;

import javax.rmi.CORBA.Util;

import com.sun.deploy.uitoolkit.impl.fx.Utils;
import com.sun.org.apache.regexp.internal.recompile;

import SelfUtil.*;
import effictive.*;

public class EffictiveTestCase{
	
	public static void main(String argv[]) {
		asdf a = new asdf();
		if (a.name() < 0) {
			
		}
		ArrayList<String> arrayList = new ArrayList<>();
		arrayList.add(null);
		arrayList.add(null);
		arrayList.add(null);
		if (arrayList.get(0).equals("asdf ")) {
			
		}
		
		Debug.print("a = " +arrayList.get(0));
	}

}
